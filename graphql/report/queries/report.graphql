query Reported($definition: ID!) {
  report(definition: $definition) {
    definition {
      id
      word
      meaning
      example
      score
      createdAt
      author {
        name
      }
    }
    reason
    message
  }
}
